<h2><%= @article.title %></h2>
<%= link_to "Back to Category's Articles", category_path(@article.categories.first) %>
<% if @article.revisions.length > 0 %>
  <p><%= @article.revisions.last.revised_content %></p>
<%else%>
    <p><%=sanitize @article.content%></p>
<%end%>
